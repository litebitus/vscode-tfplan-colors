{
  "name": "tfplan-colors",
  "displayName": "Terraform Plan Colors",
  "description": "Colorize saved terraform plan output, text or binary (rendered via terraform show): actions, gutter bars, and outline navigation",
  "version": "1.0.0",
  "publisher": "lite2073",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/litebitus/vscode-tfplan-colors.git"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "icon": "icon.png",
  "main": "./extension.js",
  "devDependencies": {
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1"
  },
  "scripts": {
    "version": "sh ./changelog.sh && git add CHANGELOG.md"
  },
  "activationEvents": [
    "onLanguage:plaintext"
  ],
  "contributes": {
    "configurationDefaults": {
      "[terraform-plan]": {
        "editor.wordWrap": "on",
        "editor.stickyScroll.defaultModel": "foldingProviderModel",
        "editor.renderLineHighlight": "all",
        "breadcrumbs.filePath": "last"
      }
    },
    "commands": [
      {
        "command": "tfplanColors.saveRenderedPlan",
        "title": "Save Rendered Plan As…",
        "category": "Terraform Plan",
        "icon": "$(save-as)"
      },
      {
        "command": "tfplanColors.copyResourceAddress",
        "title": "Copy Resource Address at Cursor",
        "category": "Terraform Plan"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "tfplanColors.saveRenderedPlan",
          "when": "resourceScheme == tfplan-show",
          "group": "navigation"
        }
      ],
      "commandPalette": [
        {
          "command": "tfplanColors.saveRenderedPlan",
          "when": "resourceScheme == tfplan-show"
        }
      ]
    },
    "customEditors": [
      {
        "viewType": "tfplanColors.binaryPlan",
        "displayName": "Terraform Plan",
        "selector": [
          {
            "filenamePattern": "**/tfplan"
          },
          {
            "filenamePattern": "**/tfplan.*"
          },
          {
            "filenamePattern": "**/*-tfplan"
          },
          {
            "filenamePattern": "**/*_tfplan"
          }
        ],
        "priority": "default"
      },
      {
        "viewType": "tfplanColors.binaryPlanOptional",
        "displayName": "Terraform Plan Preview",
        "selector": [
          {
            "filenamePattern": "*tfplan*"
          }
        ],
        "priority": "option"
      }
    ],
    "languages": [
      {
        "id": "terraform-plan",
        "aliases": [
          "Terraform Plan"
        ],
        "extensions": [
          ".tfplan"
        ],
        "filenamePatterns": [
          "*tfplan*"
        ],
        "firstLine": "^Terraform (used the selected providers|will perform)",
        "icon": {
          "light": "./fileicon.svg",
          "dark": "./fileicon.svg"
        },
        "configuration": "./language-configuration.json"
      }
    ]
  }
}
